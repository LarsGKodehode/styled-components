// 3rd Party libraries
import {
  HashRouter as router,
  Routes,
  Route,
} from 'react-router-dom'

// CSS
import styles from './App.module.css';

// Components

function App() {
  return (
    <main className={styles['App']}>
    </main>
  );
};

export default App;